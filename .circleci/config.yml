version: 2.1

orbs:
    rebar3: tsloughter/rebar3@0.5.1

workflows:
  build-test:
    jobs:
      - rebar3/compile
      - rebar3/xref:
          requires:
            - rebar3/compile
      - rebar3/dialyzer:
          requires:
            - rebar3/compile
      - rebar3/eunit:
          requires:
            - rebar3/compile
